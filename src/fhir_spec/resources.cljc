(ns fhir-spec.resources
  (:require fhir-spec.v1-8-0.resources
            fhir-spec.v3-0-1.resources))

;; Purpose of ns is to collect all resource spec declarations
;; There sure should be another way
